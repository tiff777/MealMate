namespace backend.Models.Enum
{
    public enum MealStatus
    {
        Upcoming = 0,
        Confirmed = 1,
        Completed = 2,
        Cancelled =3
    }

    public enum MealParticipateStatus
    {
        left = 0,
        joined = 1,
        pending = 2        
    }
}
